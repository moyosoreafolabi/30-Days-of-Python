# Virtual environment can help us to create an isolated or separate environment. help us to avoid conflicts in dependencies across projects. 

# To create a virtual environment, we use the venv module which is included in the standard library for Python 3.3 and above.

# To create a virtual environment, we use the command below in the terminal or command prompt
# python -m venv myenv
